import { InlineWidget } from "react-calendly";

const CalendlyWidget = () => {
  return (
    // <div style={{ height: "600px" }}>
    <InlineWidget url="https://calendly.com/adedayoaturu/30min" />
    // </div>
  );
};

export default CalendlyWidget;
